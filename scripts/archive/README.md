# Skrypty archiwalne

Jednorazowe skrypty eksploracyjne (`_probe-*`) użyte przy uzupełnianiu okładek i metadanych.
Nie są częścią codziennego workflow — zachowane na wypadek audytu historycznego.

Aktywne narzędzia CLI: `npm run audit:*`, `npm run backfill:*`, `npm run import:*`.
